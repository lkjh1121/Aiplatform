HTML 디자인

☆자바스크립트는 HTML의 확장판이다

☆도큐먼트= 바디

☆수많은 기능들을 표현하기 위헤 HTML로 표현못했던
언어들을 자바스크립트언어로 배우는것

☆약속된 규칙의 언어를 사용 

☆함수=기능 펑션( )

☆.wright() 정의되어있음

버전관리= 1.0출시 시 다음 버전에선 개발자들이 필요기능을 어필
하여 1~2년 정도 자료를 수집하여 다음 버전업때 반영하여 2.0으로 배포

☆함수량에 아는것에 량의 따라 개발자 역량이 정해짐
☆객체별 기능 외우는것 함수기능!
--------------------------------------------------------------------------

변수
값을 저장하는 메모리공간이 필요하다

var(변수) = 1(값)

=메모리 16진수 주소 설정되어있다
클라이언트 - 임시저장소(메모리) - 서버 - DB테이블에 저장
ex) a = 3 

대입연산법
☆오른쪽에 있는 값을 왼쪽에 변수에 저장

☆노하우 
사람 -> 생각 -> 클라이언트 - 프로그래밍 ->
랩 16기가 32기가 끼우면 좋음

데이터 타입
숫자 - 정수,실수
논리 true, false (boolit)
스트링 : "글자"
null 값 : 값이 없음을 표시하는 특수키워드

☆아스키코드? = 2진코드 알아두기!!
180개의 클래스 안에 180개의 (자바)오브젝트
파이썬은 패키지라 말함 

앞으로 그냥 호출된 경우 .점 앞에 객체와 기능별로() 미리 나누어 놓은것

점 앞에 document(객체).write(기능) 객체안에 기능 별로 그룹을 나눠 놓은것 (메모리 효울적 사용을위해) 객체지향 언어

객체호출하지 않고 그냥 호출내장객체(내장함수)
		
var money = 200;//전역변수
// 직접 함수(기능)을 만듬

//↓호출하지 않으면 호출되지 않음
function getMoney(){
var money = 300; //지역변수

//스크립트 안에서 브라우져 화면에 결과를 표시할 객체인 Document에 대한 설명

document.write(money); // 지역변수가 먼저 출력

document.write(this.money); // 전역변수가 먼저 출력 (this 객체자신)
}

// 함수를 호출
getMoney();

alert(): // 내장함수

반복문

if문 조건문기술이다

if만일~이라면(조건문 참이면 실행) {
	참이면 실행안됨
	조건문은에 트루 0.1사용 프로그램마다 다름 
}

for문

순서가 정해짐

for(초기값 1번은 한번만 실행. 조건문 2 2번실행 조건이 맞으면 실행싱행, 증감식 3 실랭 -> 조건문 ->조건문 맞으면 증감식 계속반복) {
	실행4
	조건문이 거짓일 경우 실행취소 문장 빠져나와서 5번으로 출력
}

for(1:a=2; 2:a<10;참이면 실행 후 증감식, 3:a++;){
	실행문 4:document.write();
} 2.4.3 순서 반복


